import { useAuth } from 'context/Auth-context';
import React from 'react';

const SignInPage = () => {
   const {user} = useAuth()
   return (
      <div>
         
      </div>
   );
};

export default SignInPage;